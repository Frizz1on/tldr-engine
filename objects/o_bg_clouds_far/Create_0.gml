event_inherited();

// Distant cloud layer — moves slowly with the camera and drifts on its own.
// Higher parallax_dist = appears further away (moves less relative to player).
parallax_dist_x = 0.15;
parallax_dist_y = 0.05;

// Slow autonomous horizontal drift (pixels per frame)
scroll_speed = 0.3;


